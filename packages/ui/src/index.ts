/**
 * Reserved for genuinely shared presentation primitives and Warm Ledger
 * design tokens (see plan Section 74, backlog item BUD-DES-001). Left
 * empty for now — do not force web and React Native to share components
 * where platform behavior differs (plan Section 23).
 */
export const UI_PACKAGE_PLACEHOLDER = true;
