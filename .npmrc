# React Native (Metro + Jest) doesn't handle pnpm's default nested
# .pnpm node_modules layout well — both assume a flatter, npm-like
# structure. Hoisting avoids transform/resolution failures in apps/mobile.
node-linker=hoisted
