# Copy to .env and adjust as needed. Local dev always points at the
# Dockerized Postgres started via `docker compose up -d` — never at the
# hosted Neon instance (see ADR-009).
DATABASE_URL="postgresql://budget_terry:budget_terry@localhost:5432/budget_terry_dev"
API_PORT=3001
LOG_LEVEL=info
